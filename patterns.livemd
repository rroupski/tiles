# Tiles notebook

```elixir
Mix.install([
  {:vix, "~> 0.5"},
  {:kino, "~> 0.7"},  # For interactive examples
  {:tiles, path: ".", env: :dev}
])

```

## Display the tiles in the room

```elixir
# The room size is in mm
Room.new!(3340, 3510, [spacer: 3, verbose: 3])

```
